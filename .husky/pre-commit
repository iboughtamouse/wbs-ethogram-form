echo "🔍 Running tests..."
npm test --silent || { echo "❌ Tests failed. Commit aborted."; exit 1; }

if grep -q "\"lint-staged\"" package.json 2>/dev/null; then
  echo "🧹 Running lint-staged..."
  npx lint-staged || { echo "❌ Lint-staged failed. Commit aborted."; exit 1; }
fi

echo "✅ Pre-commit checks passed."
